// # This are rules for prettier

const rules = {
  'prettier/prettier': [
    'error',
    {
      singleQuote: true,
      printWidth: 100,
      trailingComma: 'none'
    }
  ]
};

module.exports = rules;
